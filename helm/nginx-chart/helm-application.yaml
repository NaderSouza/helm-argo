apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nhs-helm
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/NaderSouza/helm-argo.git
    targetRevision: HEAD
    path: helm/nginx-chart
    helm:
      valueFiles:
      - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: nhs-helm-demo
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
